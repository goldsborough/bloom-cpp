#pragma once

namespace bloom {

// Need a constexpr function that determines the log_2 of a number by right shifting until we hit zero.

// constexpr
//
//
// template <typename Key>
// struct DefaultHasher {
//
// };
//
// /// Assume N is a power of two.
// template <typename Key, size_t N, size_t k, typename Hasher = DefaultHasher>
// class Bloom {
//  public:
//  private:
// };

}  // namespace bloom
